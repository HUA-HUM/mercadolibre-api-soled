export type MeliListingType = 'gold_special' | 'gold_pro';

/** Every SKU gets published under both listing types in a single request. */
export const MELI_LISTING_TYPES: MeliListingType[] = ['gold_special', 'gold_pro'];
